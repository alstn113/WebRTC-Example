import { useEffect } from 'react';

const useMediaStateChange = () => {
  // 일단 상태를 바꾼 값을 받아와야하나?
  //
  useEffect(() => {
    return;
  }, []);
  return;
};

export default useMediaStateChange;

// example
/**
 * const {  } = useMediaStateChange
 *
 */
